//arrow function

let studentList = (student) => {
  console.log(student);
}

studentList(["a", "b", "c"]);

let operators = ["ibra", "pietro", "cheery"];
 operators.map((operators) => console.log(operators));
